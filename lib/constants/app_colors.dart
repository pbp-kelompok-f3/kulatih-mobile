import 'package:flutter/material.dart';

class AppColors {
  static const indigo = Color(0xFF0F0F38);
  static const indigoDark = Color(0xFF17173F);
  static const card = Color(0xFF1C1C4A);

  static const gold = Color(0xFFD4BC4E);

  static const textLight = Colors.white70;
  static const textWhite = Colors.white;

  // ===========================
  //     BOOKING STATUS COLORS
  // ===========================
  static const statusPending = Color(0xFFE5C76B);       // soft yellow
  static const statusConfirmed = Color(0xFF4CAF50);     // green
  static const statusRescheduled = Color(0xFF9C27B0);   // purple
  static const statusCompleted = Color(0xFF2196F3);     // blue
  static const statusCancelled = Color(0xFF9E9E9E);     // grey
}
